{
  "name": "veda-app-catalog",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "VEDA App Catalog — a searchable front door to every dashboard and reference doc in veda-github-actions. Astro + Pagefind, one MDX file per entry.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "postbuild": "pagefind --site dist",
    "preview": "astro preview",
    "new-entry": "node scripts/new-entry.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.2.4",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}
