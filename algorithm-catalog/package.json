{
  "name": "algorithm-catalog",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Algorithm Catalog — filter the NASA Disasters product algorithms by hazard, date, activation event, sensor and product, and submit a new activation request as a prefilled PR. Data is committed JSON bundled at build time.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "e2e": "node scripts/e2e.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.5.4",
    "vite": "^5.4.9"
  }
}
